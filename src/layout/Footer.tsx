/**
 * Footer -- deliberately minimal. No links, no account controls, no
 * secondary navigation -- nothing in the design reference or §7.1 calls
 * for anything here beyond a quiet, low-emphasis close to the shell.
 */
export function Footer() {
  return (
    <footer className="flex shrink-0 items-center justify-center border-t border-border px-6 py-3 text-xs text-ink-faint">
      CSLabs Diagnostic Tool
    </footer>
  );
}